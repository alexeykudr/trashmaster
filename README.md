# trashmaster

This is simple game with AI,
just git clone
cd trashmaster/

virtualenv -p python3.8.5 env

source env/bin/activate
pip install -r requirements.txt

Be careful, all dependecies >= 1.5 gb :(
